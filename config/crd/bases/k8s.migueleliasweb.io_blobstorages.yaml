---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: blobstorages.k8s.migueleliasweb.io
spec:
  group: k8s.migueleliasweb.io
  names:
    kind: Blobstorage
    listKind: BlobstorageList
    plural: blobstorages
    singular: blobstorage
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Blobstorage is the Schema for the blobstorages API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BlobstorageSpec defines the desired state of Blobstorage
            properties:
              foo:
                description: Foo is an example field of Blobstorage. Edit blobstorage_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: BlobstorageStatus defines the observed state of Blobstorage
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
